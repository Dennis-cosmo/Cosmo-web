{
  "name": "@cosmo/connectors",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b .",
    "clean": "rm -rf dist",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch",
    "lint": "eslint \"src/**/*.ts\""
  },
  "dependencies": {
    "@cosmo/shared": "*",
    "axios": "^1.9.0"
  },
  "devDependencies": {
    "tsup": "^8.0.2"
  }
}
